# Plan
1. Open `https://vercel.com/account/tokens`. (Failed: Browser tool not supported on Windows)
2. Verify if logged in. If not, ask user to log in.
3. Wait for "Create Token" button to be visible.
4. Take screenshot and confirm.

# Status
Failed to open the browser because the browser tool (local chrome mode) is only supported on Linux, but the current system is running on Windows.

